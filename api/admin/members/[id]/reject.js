const { requireAdmin, send, serviceSupabase } = require('../../_admin-utils');
module.exports=async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='POST')return send(res,405,{message:'Method not allowed'});const admin=await requireAdmin(req,res);if(!admin)return;const id=String(req.query.id||'').slice(0,80);const result=await serviceSupabase(`/rest/v1/internal_member_requests?id=eq.${encodeURIComponent(id)}&status=eq.pending`,{method:'PATCH',headers:{'Content-Type':'application/json',Prefer:'return=representation'},body:JSON.stringify({status:'rejected',reviewed_at:new Date().toISOString(),reviewed_by:admin.user.email})});return result.response.ok&&result.data?.length?send(res,200,{message:'ปฏิเสธคำขอแล้ว'}):send(res,400,{message:'ไม่สามารถปฏิเสธคำขอนี้ได้'});};
